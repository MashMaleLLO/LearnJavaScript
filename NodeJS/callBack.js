const findCircleArea = n => {console.log(`Area is ${(Math.PI)*(Math.pow(n,2))}`)}
const myCallback = n2 => n2(5)

/*

getUser(function(error, users))
{
    if(error)
    {
        return ""
    }
    console.log('users)
}

getUser(
    function(error, users)
    {
        console.log('users', users)
        getProflie(users, 
            function(error, proflie)
            {
                getEmail(profile, 
                    function(error, email)
                    {
                        console.log('email)
                    })
            })
    })

*/


myCallback(findCircleArea8)